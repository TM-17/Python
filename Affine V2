#Definition des entrées
def entrée():
    a= int (input("Entrez une valeur de a:"))
    b= int (input("Entrez une valeur de b:"))
    return a,b

#Calcul fonction affine (y=ax+b)
#x réel représente un antécédent, a entier représente le coeff directeur, b entier représente l'ordonée à l'origine, y entier réel est l'image de x
def affine (a,b):
        y=a*x+b
        return y

#Sortie : Affiche y réel 
def sortie (y):
    print (y)

#Excecution du programme
a,b= entrée()
for x in range (0,10):
    y= affine(a,b)
    sortie(y)
